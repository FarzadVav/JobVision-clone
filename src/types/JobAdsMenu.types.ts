type MenuTypes = {
  link: string;
  query2: string;
  subLinks: string[]
}

type JobAdsMenuTypes = {
  id: string;
  title: string;
  query: string;
  links: MenuTypes[]
}

export default JobAdsMenuTypes