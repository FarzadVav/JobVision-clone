const Footer = () => {
  return (
    <footer className={`bg-jv-dark z-40 py-6`}>
      <div className={`wrapper flex flex-col justify-center items-center`}>
      </div>
    </footer>
  )
}

export default Footer